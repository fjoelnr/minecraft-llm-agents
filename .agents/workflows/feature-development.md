# Feature Development

1. Read `AGENTS.md` and `.agents/context-index.md`.
2. Confirm whether the change belongs in `bot-gateway/`, `orchestrator/`, or docs.
3. Update or add docs if setup, API, or architecture changes.
4. Run the narrowest relevant validation before committing.
5. Merge via `feature -> develop -> main`.
